DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
JWT_SECRET="your_jwt_secret_key"
PORT=3000

# ── Email (SMTP) ──────────────────────────────────────────────────────────────
# DEV  → Mailtrap  : sandbox.smtp.mailtrap.io  port 587  (credentials dans ton inbox Mailtrap)
# PROD → Brevo     : smtp-relay.brevo.com       port 587  (credentials dans ton compte Brevo)
SMTP_HOST="sandbox.smtp.mailtrap.io"
SMTP_PORT="587"
SMTP_USER="smtp@mailtrap.io"
SMTP_PASS="a84d9bb1aa41bb76b4665ad516df342f"
SMTP_FROM="no-reply@click-collect.fr"

# URL du frontend (utilisée dans les liens des e-mails)
FRONTEND_URL="http://localhost:5173"
